{
    "name": "cat-loaf",
    "version": "1.0.0",
    "scripts": {
        "minify-html": "html-minifier-terser --input-dir=./ --output-dir=./ --file-ext html --collapse-whitespace --remove-comments --minify-js true --minify-css true",
        "build-posts-index": "node scripts/build-posts-index.js",
        "minify": "npm run build-posts-index && npm run minify-html"
    },
    "devDependencies": {
        "clean-css-cli": "^5.6.0",
        "html-minifier-terser": "^7.2.0",
        "terser": "^5.30.0"
    },
    "dependencies": {
        "jsonwebtoken": "^9.0.2",
        "node-fetch": "^2.7.0"
    }

}
